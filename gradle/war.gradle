apply plugin: "war"

bootWar {
    mainClass = "tech.dimasyudha.infra.InfraApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
